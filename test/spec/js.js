describe('angular-spinner', function() {
  this.timeout(5000);

  beforeEach(module('angular-spinner'));

  it('Still dont know how test this directive. Please run sample', inject(function() {
    expect(true).to.be.equal(true);
  }));

});
